# If this is the first time we have been imported, we need to set up the
# package with any missing environment variables etc.
from swiftsim_utils.config import setup_environment

setup_environment()
